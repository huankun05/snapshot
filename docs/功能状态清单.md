# 截图功能状态清单

> **本文档是实时维护的单一事实源**：记录 OCR 工作区（`F:\Work\Create\OCR\screenshot`）截图功能的
> 已实现 / 待实现 / 调研结论，随开发进展持续更新。
> 规则：每完成一项就把对应条目从「待实现」移到「已实现」并注明日期与验证方式；新增需求先进「待实现」。
>
> 创建：2026-09-12 | 最后更新：2026-09-12

---

## 一、总览

| 模块 | 状态 | 说明 |
|---|---|---|
| 区域截图 | ✅ 可用且稳定 | GDI BitBlt 抓帧、悬停窗口选中 |
| 标注工具 | ✅ 可用且稳定 | 10 种工具 + 色环取色器 |
| 贴图 | ✅ 可用且稳定 | 独立 pin 窗口 |
| 截图 OCR（纯文本） | ✅ 可用 | 三引擎；识别质量待升级（见 §4.1） |
| 截图 OCR（版面还原） | 🔶 未达预期 | PP-StructureV3 路线过重，tiny 模型精度低；转向 RapidOCR 路线（见 §4） |
| 图标处理 | ❌ 未实现 | 调研结论：版面模型分类过滤，见 §4.3 |
| 长截图（滚动拼接） | 🔶 搁置中待拾起 | r19~r60 已修 40+ 项；r58~r60 未实测闭环（见 §3） |
| 翻译 | ✅ 可用 | 云 / 本地 MT 双引擎 |
| 移植回 Xiyue | ⏳ 未开始 | 依赖清单已梳理（见 §5） |

---

## 二、已实现（已验证）

### 2.1 区域截图与标注（Xiyue 同款，r60 基线）
- ✅ GDI 抓帧引擎（koffi BitBlt+GetDIBits），可选原生插件引擎（缺省回退 desktopCapturer）
- ✅ 全屏蒙版选区、悬停窗口快速选中、截图热键
- ✅ 标注 10 种工具：马赛克 / 模糊 / 噪点 / 纯色 / 矩形 / 直线 / 箭头 / 椭圆 / 画笔 / 文字
- ✅ 色环取色器；标注后保存 / 复制剪贴板 / 贴图
- ✅ 贴图窗口：缩放、拖动、尺寸标签
- 验证方式：Xiyue 生产环境长期使用（HEAD f7d3630，与工作区代码逐文件 diff 一致）

### 2.2 截图 OCR / 翻译
- ✅ 云 OCR（PP-OCR API）/ 本地 Tesseract.js / 本地 PaddleOCR Python 侧车（127.0.0.1:18765）三引擎可选
- ✅ 版面 OCR 通道（capture-ocr-local-layout）：PP-StructureV3 子进程（layout_worker.py，CPU 隔离）输出 Markdown
- ✅ 云 / 本地 MT 翻译 + 翻译浮层（段落分组 + 背景色采样回填）
- 验证方式：Xiyue 生产可用；**但版面质量用户不满意（见 §4 待实现）**

### 2.3 长截图（滚动拼接）——基础流程可用，r58~r60 待实测
- ✅ 自动匀速步进滚动 + 静止抓帧 + 帧-累积纹理匹配 + DP 最小能量缝拼接（r19~r57，用户实测确认过「拼接痕迹不明显、清晰度很高」，r52/r53 会话）
- ✅ PostMessage 滚轮注入（不依赖鼠标位置/焦点，r49）
- ✅ 空背景假谷 / 周期伪谷 / 近距滑谷 / 软帧弃拼 / 吸附式页面多滚轮补偿（r52~r59，回放回归锁定）
- ✅ 悬停干扰用户引导文案（r60，业界共识路线）
- ✅ 回归工具链：`tools/` 四件（沙盘 17/17 ✅、lapinfo 5/5 ✅ 实测通过 2026-09-12）

---

## 三、待实现 / 进行中

### 3.1 长截图拾起（优先级：高）
- ⬜ **重建 `frames/` 回放回归数据**（原 .bin 随工作区迁移丢失）。幸存数据源：
  `%LOCALAPPDATA%\Temp\xiyue-ls-debug\` 有 6 会话 22 帧 PNG（含 23:10 锚定会话完整 6 帧），
  真值已用 `tools/verify_ls_truth.py` 重算并与 docs 记录吻合（140/90×5）。
  做法：PNG→原始 RGBA .bin + meta.json，重写 `_ls_matcher_replay.js` 会话表（替代丢失的 2 会话），跑绿全部断言。
  ⏰ 数据在系统临时目录，有被清理风险，**优先做**。
- ⬜ **r58~r60 实测闭环**（步长 0.35、滑谷择优、软帧弃拼、伪谷自愈、hover 引导——均"已修待实测"状态，
  最后一次用户确认在 r53）。场景：浅色页 / 深色稀疏页 / 吸附式页面 / hover 干扰页。
- ⬜ 文档残留风险 5 项处置（矮选区重叠下限、完全重复纹理假谷、补发滚轮 2× 位移、到底误判、sharp-guard 采样伪报）
  ——详见 `长截图问题排查记录.md` 第五节。

### 3.2 OCR 排版还原重做（优先级：高，本轮主线）
- ✅ **验证 RapidOCR + PP-OCRv6 路线可行**（2026-09-12 实测，见 §4.4 数据）：
  venv_ocr 已装 rapidocr 3.9.2；small 档真实帧 warm ~1.5~1.9s/张、识别质量好；
  medium 档 13~19s/张不可用；tiny 档（现管线默认）精度低确认弃用。
- ✅ **local 引擎内核替换**（2026-09-13）：`capture-ocr-local` 先走 RapidOCR 常驻服务
  （`OCR/rapidocr_service.py`，127.0.0.1:18766，飞桨 PP-OCRv6 small/ONNX + 段落分组），
  失败自动回落 Tesseract.js；`paddleocr` 引擎选项原样保留。
- ⬜ 版面排序升级评估：GapTree（多栏/卡片 UI 强项）vs rapid-doc（PP-DocLayoutV3，文档性强），
  用真实 UI 截图对比后选型。
- ⬜ Electron 侧 `capture-ocr-local-layout` 链路回归（IPC 协议不变则只换 worker 内核）。

### 3.2a 表格识别（新增需求，2026-09-13）
- ✅ 服务端：rapidocr_service.py 新增 `/table` 端点（RapidTable + SLANet-plus，飞桨表格模型 ONNX，
  实测合成表格 2.5s，表头/行列结构还原正确）
- ✅ 主进程：新增 `capture-ocr-table` IPC（rapidOcrService.recognizeCaptureTableWithRapid）
- ✅ UI：OCR 面板新增「表格识别」按钮（按需触发，不拖慢普通 OCR）；识别成功表格 HTML 进剪贴板
  （粘贴到 Excel/WPS 即真表格，纯文本兜底为 TSV）
- ⬜ 用户实测真实表格截图（含无线表格、合并单元格、截图含表格+周边文字混合场景）
- ⬜ 可选增强：导出 .xlsx 文件、表格预览渲染进面板

### 3.2b 区域错位修复（r61，2026-09-13）
- ✅ 根因：JS 回退（desktopCapturer）模式下主进程不下发显示器布局（displays=[]）→ 渲染端走
  非合成裁剪路径，缩放公式 `naturalWidth/(W*dpr)` 在 dpr>1 时把逻辑选区当物理坐标用，
  内容向左上偏移 (1-1/dpr)。150% 缩放下偏 1/3 屏，用户实测复现。
- ✅ 修复：capture.js 两处（cropSelectionWithAnnotations / cropSelectionRaw）改为
  `naturalWidth / W`（帧像素/逻辑像素比，对物理帧与缩略帧均正确）。
  注：此 bug 在 Xiyue 无原生插件机器上同样存在，移回时一并带上。
- ⬜ 用户复测确认对齐。

### 3.3 图标处理（优先级：中）
- ✅ 问题定性（2026-09-12 实测）：UI 截图中按钮/图标文字（"编辑""继续"等）被识别为独立噪音段落
  ——文档版面模型（PP-DocLayout）按文档元素训练，对应用 UI 截图帮助有限。
- ⬜ UI 噪音过滤：启发式优先（文本长度、孤立块、框宽高比、与 UI 边缘的贴近度、置信度），
  保留原文以供"翻译覆盖"场景使用，纯文本/Markdown 通道过滤输出。
- ⬜ 可选：忽略区域功能（Umi-OCR 方案，用户手动排除水印/按钮区）。

### 3.4 移植回 Xiyue（优先级：等 3.1/3.2/3.3 完成后）
- ⬜ 按 §5 依赖清单搬移 + 接线 + 回归。

---

## 四、调研结论（2026-09-12，网络调研）

### 4.1 识别模型选档 —— 现状最大问题：我们用的 tiny 档精度不足

RapidOCR 官方博客（2026-06）CPU 实测（ExactMatch / 耗时秒）：

| 模型 | 框架 | ExactMatch | CharMatch | 耗时(s) |
|---|---|---|---|---|
| PP-OCRv6_medium | RapidOCR+ONNX | **0.8613** | 0.9491 | 0.0515 |
| PP-OCRv6_small | RapidOCR+ONNX | 0.8419 | 0.9515 | 0.0143 |
| PP-OCRv6_tiny | RapidOCR+ONNX | 0.6968 | 0.8897 | 0.0030 |
| PP-OCRv5_server | RapidOCR+ONNX | 0.8129 | 0.9431 | 0.0655 |

- **我们 PP-StructureV3 管线当前配的是 v6_tiny det/rec**（layout_worker.py 默认值）——精度垫底。
- v6_medium 以 73M 体积超越 v5_server（精度 +5.1%、检测 +4.6%），单模型统一支持中英日等 50 语言。
- 52pojie 帖的「PaddleOCR-V6-Portable」即此路线（PP-OCRv6_medium ONNX + 网页界面 + xlsx 导出），闭源，
  但证明 PP-OCRv6 ONNX CPU 推理做离线截图 OCR 完全可行。

### 4.2 排版还原 —— 两条候选路线

| 路线 | 组成 | 优势 | 代价 |
|---|---|---|---|
| **A. rapid-doc**（RapidAI） | MinerU 精简管线：PP-DocLayoutV3（自带阅读顺序）+ PP-OCRv6-small + PP-FormulaNet + 表格模型，全 ONNX CPU | OmniDocBench 90.157（超 MinerU-Pipeline 86.47 / Marker 78.44）；输出 Markdown/JSON/HTML；`pip install rapid-doc[cpu]` | 模型多、首次下载大；截图（非文档）场景可能过重 |
| **B. RapidOCR + 排序算法** | RapidOCR(PP-OCRv6) 文本块 + PP-DocLayout 版面分类 + GapTree/xy-cut 阅读 order 排序 | 轻量、可控；GapTree 为 Umi-OCR 作者开源纯算法（多栏报刊式排版强项） | 排序/结构化需自己拼装 |

- 现有 PP-StructureV3（paddle 全家桶）已知问题：CPU 内存泄漏/占用过大（PaddleOCR issue #15738）、
  整线多模型导致 CPU 极慢——这就是此前"没成功过"的原因，**方向应换成 ONNX 轻量栈**。
- 截图场景与 PDF 文档场景不同：截图多是 UI 界面（多栏卡片/侧栏/图标），PP-DocLayout 系列按文档训练，
  UI 截图效果需实测；GapTree 对 UI 多栏卡片可能更贴。

### 4.3 图标识别/过滤 —— 业界三层方案

1. **识别前**：忽略区域（Umi-OCR 方案）——适合用户手动排除水印/按钮，不适合全自动；
2. **识别中**：版面模型分类过滤——PP-DocLayout 系列原生输出 figure/icon 等元素标签，
   图标区域直接排除或占位（推荐，随 §3.2 顺带落地）；
3. **识别后**：置信度阈值 + 启发式（宽高比、笔画密度）+ 规则过滤。
- 参考：Umi-OCR 忽略区域、GapTree_Sort_Algorithm、PP-DocLayoutV2 阅读顺序解码（win-accumulation）。

### 4.4 本机实测数据（2026-09-12，venv_ocr + rapidocr 3.9.2，真实截图帧 1526×672 等 3 张）

| 配置 | warm 耗时/张 | 识别质量 |
|---|---|---|
| PP-OCRv6 small（默认，cls 关） | **~1.58s** | 好：终端命令、长段落、中英混排均准确 |
| PP-OCRv6 small（默认配置） | ~1.75s | 同上 |
| PP-OCRv6 medium | 12.8~19.1s | 仅多空格级优化，速度不可接受，弃用 |
| （对照）现管线用的 v6_tiny | 官方数据 ExactMatch 0.697 | 精度垫底，确认弃用 |

- 结论：**PP-OCRv6 small 是本机 CPU 的正确选档**；模型随 whl 内置（29.5MB），无 paddle 依赖。
- 路线 B 拼装验证（`OCR/test_layout_b.py`）：RapidOCR 文本框 + 既有 `_group_paragraphs`
  → 文本页段落分组效果好（阅读顺序正确、续行正确合并）；
  UI 截图中按钮/图标文字成为独立噪音段落 → 引出 §3.3 图标过滤项。
- 验证脚本：`OCR/test_rapidocr_v6.py`、`OCR/test_rapidocr_bench.py`、`OCR/test_layout_b.py`。

### 4.5 参考链接
- RapidOCR 集成 PP-OCRv6 记录：https://rapidai.github.io/RapidOCRDocs/main/blog/2026/06/18/rapidocr-集成-pp-ocrv6-rec-模型记录/
- RapidDoc（GitHub）：https://github.com/RapidAI/RapidDoc
- 52pojie 原帖（PaddleOCR-V6-Portable）：https://www.52pojie.cn/thread-2127382-1-1.html
- PP-StructureV3 官方教程：https://paddlepaddle.github.io/PaddleX/3.7/pipeline_usage/tutorials/ocr_pipelines/PP-StructureV3.html
- PP-StructureV3 CPU 内存问题 issue：https://github.com/PaddlePaddle/PaddleOCR/issues/15738
- GapTree 排序算法：https://github.com/hiroi-sora/GapTree_Sort_Algorithm
- Umi-OCR：https://github.com/hiroi-sora/Umi-OCR

---

## 五、移植回 Xiyue 依赖清单（摘自 README，移回时逐项核对）

- 主进程接线：`src/main/index.ts` 的 `createCaptureWindowService` / `registerCaptureIpcHandlers` /
  `registerScreenshotHotkeyIpcHandlers` / `disposeLocalOcrWorker` / 截图热键回调
- preload API：`screenshot` / `startRegionScreenshot` / `screenshotHotkeyGet|Set` / `pickFeedbackScreenshotFile`
- 配置：`storeConfig.ts` 的 `readScreenshotEngineConfig` / `readScreenshotOcrEngineConfig` /
  `readScreenshotHotkeyConfig` / `SCREENSHOT_HOTKEY_STORE_KEY`（excerpt 已摘录在
  `main/storeConfig-screenshot-excerpt.ts`）
- OCR 侧车：Python 环境 + 模型（若换 RapidOCR 路线则依赖从 paddle 全家桶缩为 onnxruntime+rapidocr，体积大减）
- 原生插件：`@eisland/windows-screenshot-helper`（可选，缺则回退）
- i18n：capture.js 内联字典（zh/en）；UI 改版不影响功能层，耦合点在 IPC 通道命名与数据格式

---

## 六、变更日志

| 日期 | 变更 |
|---|---|
| 2026-09-12 | 建档。盘点功能现状；完成排版/图标方案网络调研（§4）；确认重建 frames/ 的数据源与真值 |
| 2026-09-12 | RapidOCR + PP-OCRv6 路线实测通过（§3.2 首项 ✅，数据见 §4.4）：small 档选型确定，medium 弃用，tiny 确认精度垫底；图标问题定性为 UI 噪音过滤（§3.3） |
| 2026-09-12 | 新增独立测试服务 `OCR/ocr_test_server.py`（127.0.0.1:8766，与主项目隔离）：网页粘贴/拖入截图 → 耗时/文本框/段落/框选标注图，可切 small/tiny/medium 对比。供人工验证识别效果，验证充分后再替换主项目识别内核 |
| 2026-09-12 | 截图功能独立宿主跑通（`screenshot/app/`）：补齐提取时缺失的 `main/config/storeConfig.ts`（excerpt 补全）与 `main/services/imageTranslationService.ts`（从 Xiyue 复制）、新增 `shared/storeKeys.ts`；esbuild 打包 + Xiyue electron 启动，与 Xiyue 完全隔离（独立 userData）。全局热键 Alt+A 触发，可测区域截图/标注/贴图/长截图/OCR |
| 2026-09-13 | 升级为正式独立应用（托盘 + 生命周期修正 + 图标资产补齐）；修复 r61 区域错位（非合成裁剪路径 dpr>1 逻辑/物理混用，§3.2b）；local OCR 内核切换 RapidOCR(PP-OCRv6 small/ONNX)+Tesseract 回退（§3.2）；新增表格识别（RapidTable/SLANet-plus，OCR 面板「表格识别」按钮，HTML 复制直贴 Excel，§3.2a）；补 store:read IPC 与单实例锁 |
| 2026-09-13 | 表格体验按用户反馈改进：① 表格 HTML 默认带边框（内联样式，Excel/WPS/浏览器粘贴均可见）；② 表格结果进面板并自动预览（渲染带边框表格，mdToHtml 新增 Markdown 表格语法支持），不再"只复制不显示"；③ 复制按钮感知表格态（纯文本=TSV+HTML 双格式、Markdown=表格源+HTML）；④ 文字识别输出格式化 Markdown（标题/列表启发式），Markdown 与纯文本不再相同 |
| 2026-09-13 | OCR 分体按钮（主体=文字识别 / 右下角 ▾=表格识别菜单，交互对齐翻译按钮）；识别结果默认进预览态；预览链接 Ctrl+点击系统浏览器打开；排版启发式 v3：行内序号切分（仅多行合并框生效）、标题判定加三重过滤（贴上沿不算/须含实际内容/Markdown 符号开头不算）、列表块续行合并；修复面板编辑态/预览态同时显示的 bug；表格识别补"识别中"蒙版提示。PP-DocLayoutV3 基准已测：124.5MB / CPU 570ms，作为下一大项（版面混排+智能识别） |
| 2026-09-13 | ① toast 遮挡修复：toast 复用"识别中"状态条定位（整块盖住选区）导致原图无法对比——改为内容自适应小胶囊悬在选区上沿之外，表格提示 2.6s 自动消失；② 表头并格修复：SLANet 系模型对无竖线表头普遍把整行并成 1 格（ppstructure_zh/unitable 均验证过，unitable 需 torch 弃用）——服务端新增几何后处理：数据行单元格 x 边界做列边界，表头条带重跑词级 OCR 按坐标归列重建表头（合成样例验证 4×3 全对）。残留：完全无竖线的数据行仍可能并格（模型固有限制） |
